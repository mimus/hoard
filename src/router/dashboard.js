import Dashboard from '@/components/Dashboard'

var routes = [
  {
    path: '/',
    name: 'Dashboard',
    component: Dashboard,
    meta: {
      bcLabel: 'Dashboard'
    }
  }
]

export default routes
